slskdN is a self-hosted Soulseek client and server with a web interface. It
connects to the Soulseek peer-to-peer network and lets administrators manage
searches, downloads, transfers, and optional shared directories from a browser.

The app package keeps the web interface behind YunoHost's reverse proxy and
supports installation on a subpath.
